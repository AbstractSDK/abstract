[alias]
wasm = "build --release --target wasm32-unknown-unknown"
unit-test = "test --lib"
schema = "run --bin schema --features schema"
# Use the command in the justfile to deploye
internal-deploy = "run --bin deploy --features interface"
